/*package home12;

import java.util.ArrayList;
import java.util.List;

public class Application {
    public static void main(String[] args) { // все классы наследуются от класса object по дефолту
        //People ivan = new People(); // создали объект ivan типа People, он уникален
       // People p = new People();
       // ivan.setName("Ivan"); // так работает сеттер
        //ivan.setAge(30);
        //String name = ivan.getName(); // из объекта ivan получим его имя
        //People another = new People(); // ещё один объект типа People, но другой
        //System.out.println(ivan.name);// тут ещё ничего нужного не выведет, дефолтное значение выдаст
        //System.out.println(ivan.age);
        //System.out.println(ivan);// toString - метод, который приводит к строке
        //System.out.println(ivan.getName() + " " + ivan.getAge()); // выводим отдельно имя и возраст
        //Customer customer = new Customer();
        //People customer = new Customer();
        /*customer.setName("Обэма");
        customer.setAge(22);


        //customer.whoIam();
        People cust = new People("Оляшка", 15);
        People anothr = new People("Ку");

        People employee = new Employee();
        //Employee employee = new Employee();
        /*employee.setName("Работяга");
        employee.setAge(28);


        //employee.whoIam();

        List<People> somePeoples = new ArrayList<>();
        somePeoples.add(customer);
        somePeoples.add(employee);
        whoAreYou(somePeoples);





        //ivan.name = "Ivan"; // присвоили объекту ivan имя
        //ivan.age = 25;// присвоили объекту ivan возраст
        //System.out.println(ivan.name);// а тут выведет нужное уже, ибо значения присвоили
        //System.out.println(ivan.age);// и тут выведет
        /*ООП, суть в том, что у нас есть классы из которых создаются объекты и они между собой могут работать
        То есть один метод будет вызывать методы другого
        3 парадигмы: Полиморфизм - ;
        Инкапсуляция - Мы в классе объединяем и данные, и методы, которые с ними работают и скрываем реализацию от пользователя;
        к инкапсуляции относятся геттеры и сеттеры;

        Наследование -  ;






    }
    public  static void whoAreYou(List<People> peoples){
        for(People element : peoples){
            element.whoIam();
        }

    }
}
*/